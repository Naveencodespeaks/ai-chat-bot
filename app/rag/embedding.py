
# app/rag/embedding.py

def embed_query(text: str):
    """
    Generate embedding for a query.
    This function acts as a single abstraction point
    for all embedding logic.
    """

    # TEMPORARY PLACEHOLDER (safe)
    # Replace later with OpenAI / Azure / local model
    return [0.0] * 768
